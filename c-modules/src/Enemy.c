#include "../header/Enemy.h"


Enemy* newEnemy(const char* json)/*(
    bool isAlpha,
    uint8_t maxHp,
    uint8_t size,
    uint8_t hitDmg,
    uint16_t rewardScore,
    float bulletShootableCnt,
    Vector2* pos,
    Vector2* speed
)*/{
    Enemy* enemy = malloc(sizeof(Enemy));
    return enemy;
}